import { createSlice } from '@reduxjs/toolkit'

export const lightTrapLogSlice = createSlice({
  name: 'lighttrap',
  initialState: {
    lightTrapLogs: [],
  },
  reducers: {},
})

export default lightTrapLogSlice.reducer
